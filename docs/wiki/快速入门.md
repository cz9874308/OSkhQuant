# 快速入门

<cite>
**本文档引用的文件**   
- [README.md](file://README.md)
- [项目文件说明.md](file://项目文件说明.md)
- [GUIkhQuant.py](file://GUIkhQuant.py)
- [khFrame.py](file://khFrame.py)
- [khConfig.py](file://khConfig.py)
- [khQTTools.py](file://khQTTools.py)
- [khTrade.py](file://khTrade.py)
- [khQuantImport.py](file://khQuantImport.py)
- [MyTT.py](file://MyTT.py)
- [strategies/双均线精简_使用khMA函数.py](file://strategies/双均线精简_使用khMA函数.py)
- [SettingsDialog.py](file://SettingsDialog.py)
</cite>

## 目录
1. [环境依赖与前置条件](#环境依赖与前置条件)
2. [项目目录结构解析](#项目目录结构解析)
3. [配置MiniQMT客户端](#配置miniqmt客户端)
4. [启动与连接](#启动与连接)
5. [端到端回测示例：双均线策略](#端到端回测示例双均线策略)
6. [理解策略交互逻辑](#理解策略交互逻辑)
7. [常见问题与初始化检查](#常见问题与初始化检查)

## 环境依赖与前置条件

在部署和运行OSkhQuant系统之前，必须满足以下环境依赖和前置条件。

首先，操作系统必须为 **Windows 10 或更高版本的64位系统**。该系统深度依赖于券商提供的MiniQMT客户端，而MiniQMT主要运行于Windows平台，因此Windows是唯一官方支持的操作系统。

其次，必须安装 **MiniQMT客户端**。这是OSkhQuant系统运行的绝对前提。用户需要从开户的券商处下载并安装最新版的MiniQMT客户端，并确保能够正常登录账户。OSkhQuant通过MiniQMT获取行情数据和执行交易指令。

最后，需要安装 **Microsoft Visual C++ Redistributable (x64)**。为了确保系统图形界面和部分依赖库的正常工作，建议安装 `Microsoft Visual C++ 2015-2022 Redistributable (x64)`。

**Section sources**
- [README.md](file://README.md#L1-L30)
- [README.md](file://README.md#L30-L50)

## 项目目录结构解析

OSkhQuant项目的目录结构清晰，各模块职责分明。理解其结构有助于更好地管理和使用系统。

项目根目录下包含一个 `strategies` 文件夹，其中存放了所有策略脚本，例如 `RSI策略.py`、`双均线多股票_使用MA函数.py` 等。这些 `.py` 文件是用户编写和存放量化策略的核心位置。

根目录下的核心文件包括：
*   **`GUIkhQuant.py`**: 量化交易平台的主界面和核心控制器，负责策略执行、回测、账户管理和日志显示。
*   **`khFrame.py`**: 量化交易框架核心，包含策略执行引擎、回测框架和与交易接口的桥接。
*   **`khConfig.py`**: 配置管理模块，负责解析和管理系统的配置文件。
*   **`khQTTools.py`**: 量化交易工具集，提供数据获取、技术指标计算等工具函数。
*   **`khTrade.py`**: 交易管理模块，负责订单管理、交易成本计算和持仓管理。
*   **`khQuantImport.py`**: 统一导入模块，简化策略开发时的导入过程。
*   **`MyTT.py`**: 技术指标计算库，包含MA、RSI、MACD等常用指标。
*   **`SettingsDialog.py`**: 系统设置对话框，用于配置软件参数。

**Section sources**
- [项目文件说明.md](file://项目文件说明.md#L1-L200)
- [README.md](file://README.md#L1-L20)

## 配置MiniQMT客户端

首次运行OSkhQuant系统时，必须正确配置MiniQMT客户端的路径，这是系统与MiniQMT建立连接的关键步骤。

1.  启动软件后，在主界面顶部工具栏点击 **设置** 按钮，打开“软件设置”对话框。
2.  在“软件设置”对话框中，找到 **客户端设置** 区域。这里需要配置两个至关重要的路径：
    *   **miniQMT客户端路径**: 指向MiniQMT的主程序文件 `XtItClient.exe`。点击“浏览...”按钮，进入MiniQMT安装目录的 `bin.x64` 文件夹进行选择。
    *   **miniQMT路径**: 指向MiniQMT的用户数据文件夹 `userdata_mini`。点击“浏览...”按钮，选择该文件夹。
3.  完成两个路径的配置后，点击“保存设置”按钮。

**Section sources**
- [README.md](file://README.md#L1-L30)
- [SettingsDialog.py](file://SettingsDialog.py#L1-L649)

## 启动与连接

OSkhQuant系统与MiniQMT是两个独立的软件，它们之间的连接有严格的启动顺序。

1.  **第一步：启动并登录MiniQMT**。首先打开您的券商MiniQMT客户端，并成功登录您的账户。登录时，请务必勾选“极简模式”。
2.  **第二步：启动OSkhQuant系统**。在MiniQMT运行并登录成功后，再启动OSkhQuant系统。

软件启动后，主界面右上角会有一个 **MiniQMT状态指示灯**：
*   **🟢 绿色**: 表示连接成功，可以进行回测等操作。
*   **🔴 红色**: 表示连接失败。请检查是否先启动了MiniQMT、MiniQMT是否已登录，以及在“设置”中配置的两个路径是否完全正确。

**Section sources**
- [README.md](file://README.md#L1-L30)

## 端到端回测示例：双均线策略

本节将通过一个完整的示例，指导您从创建新工程到查看回测结果的全过程。

1.  **创建新工程**：在主界面点击“加载配置”，选择一个内置的示例工程（如 `demoMACD.kh`）作为起点，然后通过“配置另存为”将其保存到您的用户策略目录（如 `C:\Users\[用户名]\AppData\Local\KhQuant\strategies\`）中，以创建一个新工程。
2.  **选择策略模板**：在左侧“策略配置”区域，点击“选择策略文件”，从 `strategies` 文件夹中选择 `双均线精简_使用khMA函数.py` 作为策略模板。
3.  **配置回测参数**：
    *   在“回测参数”中，设置“回测时间设置”的开始日期和结束日期。
    *   在“股票池设置”中，通过“自选清单”输入您想回测的股票代码，例如 `002945.SZ,华林证券`。
    *   在“数据设置”中，选择“周期类型”为 `1m`（1分钟）或 `1d`（日线）。
4.  **补充回测数据**：点击顶部工具栏的“数据”按钮，打开数据中心。分别选择 `1分钟` 和 `1日` 周期，设置相同的日期范围，然后点击“补充数据”按钮，确保策略所需的所有历史数据都已下载到本地。
5.  **执行回测**：确认所有配置无误后，点击“开始运行”按钮。在“系统日志”面板中观察回测进度，底部状态栏会显示详细的进度条。
6.  **查看结果**：回测完成后，回测报告会自动弹出。您也可以随时点击“打开回测指标”按钮来查看报告，其中包含核心绩效指标、资金曲线图和详细的交易记录。

**Section sources**
- [README.md](file://README.md#L1-L30)
- [strategies/双均线精简_使用khMA函数.py](file://strategies/双均线精简_使用khMA函数.py#L1-L31)

## 理解策略交互逻辑

策略文件通过一个标准的接口与OSkhQuant框架进行交互。核心是 `khHandlebar` 函数，它在每个数据周期（如每个K线形成时）被调用。

以下代码片段展示了双均线策略的核心逻辑：

```python
def khHandlebar(data: Dict) -> List[Dict]:
    """策略主逻辑，在每个K线或Tick数据到来时执行"""
    signals = []
    stock_code = khGet(data, "first_stock")
    current_price = khPrice(data, stock_code, "open")
    current_date_str = khGet(data, "date_num")
  
    ma_short = khMA(stock_code, 5, end_time=current_date_str)
    ma_long = khMA(stock_code, 20, end_time=current_date_str)
      
    has_position = khHas(data, stock_code)
  
    if ma_short > ma_long and not has_position:
        signals = generate_signal(data, stock_code, current_price, 1.0, 'buy', f"5日线({ma_short:.2f}) 上穿 20日线({ma_long:.2f})，全仓买入")

    elif ma_short < ma_long and has_position:
        signals = generate_signal(data, stock_code, current_price, 1.0, 'sell', f"5日线({ma_short:.2f}) 下穿 20日线({ma_long:.2f})，全仓卖出")

    return signals
```

在 `data` 字典中，包含了丰富的信息：
*   **`__current_time__`**: 当前的时间戳、日期和时间。
*   **`__account__`**: 账户的现金、总资产等信息。
*   **`__positions__`**: 当前的持仓信息。
*   **`__stock_list__`**: 股票池列表。
*   **`__framework__`**: 框架实例，可用于更高级的操作。

`khGet`, `khPrice`, `khHas` 等便捷函数（定义在 `khQuantImport.py` 中）可以简化对这些数据的访问。

**Section sources**
- [strategies/双均线精简_使用khMA函数.py](file://strategies/双均线精简_使用khMA函数.py#L1-L31)
- [khQuantImport.py](file://khQuantImport.py#L1-L521)
- [khFrame.py](file://khFrame.py#L1-L2679)

## 常见问题与初始化检查

为确保系统顺利运行，请在首次使用时进行以下检查：

*   **路径配置**：确认在“设置”中配置的两个MiniQMT路径（客户端路径和数据路径）是否正确无误。
*   **启动顺序**：务必遵循“先启动并登录MiniQMT，再启动OSkhQuant”的顺序。
*   **数据补充**：在运行回测前，务必通过“数据”模块补充所需的分钟和日线数据。即使回测能启动，缺少数据也会导致结果不准确。
*   **策略文件位置**：强烈建议将策略文件和 `.kh` 配置文件保存在用户策略目录或自定义目录中，避免因软件升级而丢失。
*   **连接状态**：检查主界面右上角的MiniQMT状态指示灯是否为绿色。

**Section sources**
- [README.md](file://README.md#L1-L30)
- [README.md](file://README.md#L30-L50)